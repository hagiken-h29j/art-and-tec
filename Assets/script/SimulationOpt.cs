using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SimulationOpt : MonoBehaviour {

	// Use this for initialization
	void Start () {
        //Dictionary<string, int> opt = Option.GetOptData();
        //Debug.Log("Set : (range, " + opt["range"] + ")");
        //Debug.Log("Set : (level, " + opt["level"] + ")");

    }
	
	// Update is called once per frame
	void Update () {
		
	}
}
